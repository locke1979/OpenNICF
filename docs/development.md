# Development

Run `python -m pytest`. Install `pip install -e '.[test,qwen]'` for QwenAgent integration work. The bridge uses `SUBAGENT_RUNNER_COMMAND` as an adapter boundary; undocumented OpenClaw internals must not be embedded in OpenNICF.

