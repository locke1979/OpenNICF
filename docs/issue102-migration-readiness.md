# Issue #102 migration readiness

Status: `TEXT_READY_FOR_GATED_EVALUATION`; `VL_ALIASED_OR_UNSUPPORTED`.

## Configuration inventory

| Consumer | Endpoint | Text model | VL model | Activation |
|---|---|---|---|---|
| production knowledge/ingestion | `http://192.168.1.137:1234` | `text-embedding-qwen3-embedding-4b` | `qwen.qwen3-vl-embedding-2b` | `OPENNICF_EMBEDDING_REMOTE_ENABLED=false` |
| Issue #102 evaluation runner | `http://192.168.1.137:1234` | exact model passed explicitly | exact model passed explicitly | text preflight required; VL disabled |
| CT305 worker | existing local worker configuration | unchanged | n/a | untouched |
| CT308 worker | isolated local CUDA/llama.cpp path | historical evaluation only | historical evaluation only | HTTP benchmark does not use it |

The committed example is `deploy/model-router.env.example`; protected runtime
files and tokens remain outside Git. There is no automatic local model download,
model alias resolution, or configured-endpoint CPU fallback.

## Contract

Text responses must identify exactly
`text-embedding-qwen3-embedding-4b`, contain one finite 2,560-D vector per
input in order, and pass deterministic/list ordering checks. Storage vectors
are derived as `native[0:768]` followed by L2 normalization and use the new
endpoint-bound space `ISSUE102_HTTP_TEXT_QWEN3_4B_2560_PREFIX768_V1`.

VL must independently prove model identity, multimodal behavior, native
dimension, pooling, and preprocessing. The current response identifies the
text model and is rejected. VL production and evaluation remain disabled.

## Safety state

- `PRODUCTION_INDEX_MUTATED=false`
- `PRODUCTION_DEPLOYMENT=false`
- `REEMBEDDING=false`
- `CT305_TOUCHED=false`
- `CT308_LOCAL_INFERENCE=OUT_OF_SCOPE`
- endpoint-host RAM, queue capacity, RSS, GPU memory, and swap: `UNVERIFIED`
- canonical Issue #101 and T00-T07 artifacts: unchanged

Readiness evidence is generated by
`tools/preflight_issue102_endpoint.py` and recorded in
`evaluation/results/issue102-endpoint-preflight-2026-08-23.json`.
