#!/usr/bin/env python3
import json
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "src"))

from opennicf.ops.secrets import inventory
print(json.dumps({"schema_version": "opennicf.config-inventory/v1", "secrets": inventory(), "values": "omitted"}, sort_keys=True))
