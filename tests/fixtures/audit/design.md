# Billing failure handling

The billing service must preserve provenance for every incident correlation.
When the investigation needs live database contents, request controlled on-prem
verification through the broker interface. Do not infer database rows from logs.
